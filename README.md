# Setup a Kubernetes cluster using Vagrant + Fedora Atomic

## Generating the certificates ([Depends on cfssl](https://github.com/cloudflare/cfssl]))

    bash cfssl/generate-certs.sh

## Start the cluster

   vagrant up
